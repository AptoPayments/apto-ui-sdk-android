<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/menu_container_activate_card"
    android:layout_width="wrap_content"
    android:layout_height="32dp"
    android:orientation="horizontal"
    android:paddingEnd="10dp"
    tools:background="@android:color/black">

    <com.aptopayments.sdk.ui.views.AptoTextView
        android:id="@+id/tv_menu_activate_physical_card"
        android:layout_width="wrap_content"
        android:layout_height="18dp"
        android:layout_gravity="center"
        android:layout_marginEnd="6dp"
        android:gravity="center_vertical"
        android:paddingLeft="8dp"
        android:paddingRight="8dp"
        app:localize="manage_card.activate_physical_card.top_bar_item.title"
        tools:textColor="@android:color/white" />

    <ImageView
        android:id="@+id/menu_image"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_marginTop="3dp"
        android:src="@drawable/ic_activate_physical_card" />

</LinearLayout>
